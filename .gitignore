# Локальный конфиг мастера (список воркеров, ключи — не коммитить)
config/master.yaml

# PID-файл GUI (создаётся при --start-worker, блокирует git pull если не игнорировать)
.chaboss_gui.pid

# Временные/автосейв файлы конфигов (редакторы)
config/*.yaml~
config/*.yml~
config/#*#
config/.#*
config/*.swp
config/*.swo
config/*.tmp
config/*.bak

# Виртуальное окружение
.myvenv/
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
**/__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
*.egg-info/
.eggs/
dist/
build/
*.egg
